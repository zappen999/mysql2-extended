{
  "name": "mysql2-extended",
  "version": "0.2.1",
  "description": "Thin convenience layer for mysql2 library.",
  "main": "src/index.js",
  "scripts": {
    "test": "./node_modules/.bin/jest",
    "test:watch": "./node_modules/.bin/jest --watchAll",
    "coverage:generate": "./node_modules/.bin/jest --coverage",
    "coverage:publish": "./node_modules/.bin/jest --coverage --coverageReporters=text-lcov | npx coveralls && rm -rf coverage/"
  },
  "author": "Johan Kanefur <johan@kanefur.se>",
  "license": "GPL-3.0",
  "devDependencies": {
    "@mobajl/eslint-config": "^1.1.2",
    "coveralls": "^3.1.0",
    "jest": "^26.4.2",
    "mysql2": "^2.2.1"
  }
}
